/* Rule untuk mengecek apakah di sekitarnya ada enemy atau shop atau dungeon */

/* Check surroundings */

/* Interact with store */
store :-
    game_start(true),
    in_battle(false),
    isNear("S"), !,
    open_store.

store :-
    game_start(true),
    in_battle(true), !,
    write("Kalm down, an enemy is right in front of you. Use \'naon.\' to defeat your enemy.").

store :-
    game_start(true),
    (\+ isNear("S")), !,
    write("You\'re\ not in a store. Use \'map.\' to check where the store is").

store :-
    game_start(false), !,
    write("The game hasn\'t even started yet. Use \'start.\' to start the game").

/* Interact with dungeon */
/* Need fix? Check left, right, top, bottom locations (gak nimpa dungeon). */

dungeon :-
    game_start(true),
    in_battle(false),
    isNear("D") !,
    in_dungeon.
    /* belum bikin in_dungeon */

dungeon :-
    game_start(true),
    in_battle(true), !,
    write("Must not take on two enemies at once. Defeat your current foe to proceed.").

dungeon :-
    game_start(true),
    (\+ isNear("D")), !,
    write("You\'re\ not in the dungeon. Use \'map.\' to check where the store is").

dungeon :-
    game_start(false), !,
    write("The game hasn\'t\ even started yet. Use \'start.\' to start the game").

/* Check if any enemies around */
/* Ada di move.pl(?) */
